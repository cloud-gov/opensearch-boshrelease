#!/usr/bin/env bash

unzip awscli-exe-linux-x86_64.zip -d "$BOSH_INSTALL_TARGET"
mkdir "$BOSH_INSTALL_TARGET/bin"
cp -R "$BOSH_INSTALL_TARGET/aws/dist/*" "$BOSH_INSTALL_TARGET/bin"
chmod +x "$BOSH_INSTALL_TARGET/bin/aws"
