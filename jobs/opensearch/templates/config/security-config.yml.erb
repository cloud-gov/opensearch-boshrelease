---
_meta:
  type: "config"
  config_version: 2

config:
  dynamic:
    authc:
      cert_auth:
        description: "Authenticate via client certificate"
        http_enabled: true
        transport_enabled: true
        order: 4
        http_authenticator:
          type: clientcert
          challenge: true
        authentication_backend:
          type: noop
