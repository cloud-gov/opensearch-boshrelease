#!/bin/bash

if ! [[ $(ls -1 /var/vcap/packages/opensearchDashboards/plugins | wc -l) == 0 ]]; then
  rm -rf /var/vcap/packages/opensearchDashboards/plugins/*
fi

cd /var/vcap/packages/opensearchDashboards
chown -R vcap:vcap config data plugins
